[package]
name = "zk-circuits"
version = "0.1.0"
edition = "2021"
[workspace]
members = ["methods/guest"]

[dependencies]
risc0-zkvm = "1.0"

[build-dependencies]
risc0-build = "1.0"
risc0-zkvm = { version = "1.0", features = ["prove"] }
zk_methods = { path = "methods/guest" }
